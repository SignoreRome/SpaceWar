/*package MainGame.Flot;

import Engine.NameAndSurname;
import MainGame.Persons.Officer;

public class ShipsFactory {
    public Ships getSpaceShip(ShipTypes type) {
        switch (type) {
            case CARGO:
                return new SpaceCargoShip(this.getClass().getSimpleName(), cre);
            case SCOUT:
                return new SpaceScoutShip();
            case MILITARY:
                return new SpaceMilitaryShip();
            default:
                throw new IllegalArgumentException("Неверный");
        }
    }
}*/
